# AGENTS.md

## Cursor Cloud specific instructions

This is a **documentation-only repository** containing Markdown research notes about AI agent platforms. There is:

- No application code, no services to run, no tests to execute, no build system.
- No dependencies to install (`package.json`, `requirements.txt`, etc. do not exist).
- No lint, build, or test commands.
- The entire repo consists of two Markdown files: `README.md` and `RESEARCH.md`.

Future agents should not attempt to start dev servers, run tests, or install dependencies — there is nothing to run.
